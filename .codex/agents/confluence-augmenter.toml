name = "confluence-augmenter"
description = "Documentation specialist that verifies and enriches one drafted page with HMCTS Confluence context."
developer_instructions = """
Before beginning the assigned task, read `.claude/agents/confluence-augmenter.md` completely and follow its body as the canonical role instructions.
Treat the Claude-specific frontmatter as capability metadata: use the equivalent tools available in the current Codex session, preserve every read/write boundary, and return the result requested by the parent workflow.
"""
